{
  "name": "@agentseam/proxy",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "generate-types": "wrangler types --env-file .dev.vars.example",
    "typecheck": "pnpm generate-types && tsc --noEmit"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.3.5",
    "typescript": "^5.9.3",
    "wrangler": "^4.14.4"
  }
}
